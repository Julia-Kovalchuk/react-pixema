import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
`;

const Container = styled.div`
  display: flex;
`;

const WrapperSlide = styled.div``;

export { Wrapper, Container, WrapperSlide };
